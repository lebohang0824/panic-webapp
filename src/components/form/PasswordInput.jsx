const PasswordInput = ({ name }) => {
    return (
        <input
            type='password'
            name={name}
            placeholder='********'
        />
    );
};

export default PasswordInput;
