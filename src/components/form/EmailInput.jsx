const EmailInput = ({ name, placeholder }) => {
    return (
        <input
            type='email'
            name={name}
            placeholder={placeholder}
        />
    );
};

export default EmailInput;
